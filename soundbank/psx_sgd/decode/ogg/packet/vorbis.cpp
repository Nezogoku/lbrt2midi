#include <algorithm>    //For std::min(), std::max()
#include <cmath>        //For std::floor()
#include <cstdint>      //For uintX_t, intX_t, INT16_MIN, INT16_MAX
#include <vector>       //For std::vector
#include "vorbis_utils.hpp"
#include "vorbis_tables.hpp"
#include "vorbis.hpp"

using std::round;
using std::max;
using std::min;
using std::floor;
using std::vector;


//Chugging along because I want to do it myyyy way
std::vector<int16_t> vorbDecode(char *vrbData, uint32_t vrbSize, int ch, uint32_t rt) {
	vector<int16_t> wavData;



    return wavData;
}
